# Copy to hosts.ini (git-ignored) and paste the IPs from `terraform output node_ips`.
# Fresh Hetzner servers accept root over SSH; the base role then creates the
# `ops` user and disables root login.
# IPs below are documentation placeholders (TEST-NET-3), not real hosts.

[docker]
app-1 ansible_host=203.0.113.10

[control_plane]
# cp-1 ansible_host=203.0.113.20

[workers]
# worker-1 ansible_host=203.0.113.21
# worker-2 ansible_host=203.0.113.22

[k8s:children]
control_plane
workers

[all:vars]
ansible_user=root
